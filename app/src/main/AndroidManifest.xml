<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icon_hq"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_hq"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.MyApplication"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity android:name=".Activity.HomeBaseActivity" android:label="Home View"/>
        <activity android:name=".Activity.SignupActivity" android:label="Signup View"/>
        <activity android:name=".Activity.LoginActivity" android:label="Login View"/>
        <activity android:name=".Activity.NewsDetailActivity" android:label="News Detail View"/>
        <activity android:name=".Activity.ProfileActivity" android:label="Profile View" android:theme="@style/TransparentActivity" />
        <activity android:name=".Activity.UserInfoEditActivity" android:label="Edit Profile View" />
        <activity
            android:name=".Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>